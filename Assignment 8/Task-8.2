def assign_grade(score):
    """
    Assigns a letter grade based on the given score.

    Args:
        score (int or float): The numerical score to be graded.

    Returns:
        str: The assigned letter grade (A, B, C, D, F) or an error message
             for invalid scores.
    """
    if not isinstance(score, (int, float)):
        return "Invalid input: Score must be a number."
    if score < 0 or score > 100:
        return "Invalid score: Score must be between 0 and 100."
    elif score >= 90:
        return "A"
    elif score >= 80:
        return "B"
    elif score >= 70:
        return "C"
    elif score >= 60:
        return "D"
    else:
        return "F"

# Run the AI-generated test cases
test_cases = {
    "A": [100, 95, 90],
    "B": [89, 85, 80],
    "C": [79, 75, 70],
    "D": [69, 65, 60],
    "F": [59, 30, 0],
    "Invalid score: Score must be between 0 and 100.": [-5, 101],
    "Invalid input: Score must be a number.": ["eighty", None, [75], True, False]
}

print("--- Running Test Cases for assign_grade ---")
for expected_output, inputs in test_cases.items():
    for score_input in inputs:
        actual_output = assign_grade(score_input)
        status = "PASSED" if actual_output == expected_output else "FAILED"
        print(f"Input: {score_input} (Type: {type(score_input).__name__}), Expected: '{expected_output}', Actual: '{actual_output}' - {status}")
